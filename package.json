{
  "name": "@riots/utils",
  "version": "1.0.0",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jannis2301/utils.git"
  },
  "scripts": {
    "start": "vite --host",
    "test": "vitest run",
    "test:unit": "vitest run --coverage",
    "coverage": "vitest run --coverage",
    "build": "tsc && vite build"
  },
  "devDependencies": {
    "@vitest/coverage-istanbul": "^1.1.0",
    "happy-dom": "^12.10.3",
    "typescript": "^5.2.2",
    "vite": "^5.0.8",
    "vite-plugin-dts": "^3.6.4",
    "vitest": "^1.1.0"
  },
  "files": [
    "./dist"
  ],
  "exports": {
    ".": {
      "types": "./dist/types/index.d.ts",
      "default": "./dist/utils.js"
    }
  }
}
